---
applyTo: '**'
---
# Project general coding standards

## working environment
- Use conda env `efnet` to run the project
- if gpu is needed, first check which cuda devices are available(memory usage less than 2GB), then set the environment variable `CUDA_VISIBLE_DEVICES` to the corresponding device ids.
